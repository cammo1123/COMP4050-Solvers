// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



export class BoxType implements flatbuffers.IUnpackableObject<BoxTypeT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BoxType {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBoxType(bb:flatbuffers.ByteBuffer, obj?:BoxType):BoxType {
  return (obj || new BoxType()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBoxType(bb:flatbuffers.ByteBuffer, obj?:BoxType):BoxType {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BoxType()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

reference():string|null
reference(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
reference(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

width():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

length():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

depth():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

maxWeight():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

boxWeight():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

active():boolean|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : null;
}

maximumBoxes():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : null;
}

static startBoxType(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addReference(builder:flatbuffers.Builder, referenceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, referenceOffset, 0);
}

static addWidth(builder:flatbuffers.Builder, width:number) {
  builder.addFieldInt32(1, width, 0);
}

static addLength(builder:flatbuffers.Builder, length:number) {
  builder.addFieldInt32(2, length, 0);
}

static addDepth(builder:flatbuffers.Builder, depth:number) {
  builder.addFieldInt32(3, depth, 0);
}

static addMaxWeight(builder:flatbuffers.Builder, maxWeight:number) {
  builder.addFieldFloat32(4, maxWeight, null);
}

static addBoxWeight(builder:flatbuffers.Builder, boxWeight:number) {
  builder.addFieldFloat32(5, boxWeight, null);
}

static addActive(builder:flatbuffers.Builder, active:boolean) {
  builder.addFieldInt8(6, +active, null);
}

static addMaximumBoxes(builder:flatbuffers.Builder, maximumBoxes:number) {
  builder.addFieldInt32(7, maximumBoxes, null);
}

static endBoxType(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createBoxType(builder:flatbuffers.Builder, referenceOffset:flatbuffers.Offset, width:number, length:number, depth:number, maxWeight:number|null, boxWeight:number|null, active:boolean|null, maximumBoxes:number|null):flatbuffers.Offset {
  BoxType.startBoxType(builder);
  BoxType.addReference(builder, referenceOffset);
  BoxType.addWidth(builder, width);
  BoxType.addLength(builder, length);
  BoxType.addDepth(builder, depth);
  if (maxWeight !== null)
    BoxType.addMaxWeight(builder, maxWeight);
  if (boxWeight !== null)
    BoxType.addBoxWeight(builder, boxWeight);
  if (active !== null)
    BoxType.addActive(builder, active);
  if (maximumBoxes !== null)
    BoxType.addMaximumBoxes(builder, maximumBoxes);
  return BoxType.endBoxType(builder);
}

unpack(): BoxTypeT {
  return new BoxTypeT(
    this.reference(),
    this.width(),
    this.length(),
    this.depth(),
    this.maxWeight(),
    this.boxWeight(),
    this.active(),
    this.maximumBoxes()
  );
}


unpackTo(_o: BoxTypeT): void {
  _o.reference = this.reference();
  _o.width = this.width();
  _o.length = this.length();
  _o.depth = this.depth();
  _o.maxWeight = this.maxWeight();
  _o.boxWeight = this.boxWeight();
  _o.active = this.active();
  _o.maximumBoxes = this.maximumBoxes();
}
}

export class BoxTypeT implements flatbuffers.IGeneratedObject {
constructor(
  public reference: string|Uint8Array|null = null,
  public width: number = 0,
  public length: number = 0,
  public depth: number = 0,
  public maxWeight: number|null = null,
  public boxWeight: number|null = null,
  public active: boolean|null = null,
  public maximumBoxes: number|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const reference = (this.reference !== null ? builder.createString(this.reference!) : 0);

  return BoxType.createBoxType(builder,
    reference,
    this.width,
    this.length,
    this.depth,
    this.maxWeight,
    this.boxWeight,
    this.active,
    this.maximumBoxes
  );
}
}
