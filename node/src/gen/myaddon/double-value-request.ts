// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Config, ConfigT } from '../myaddon/config.js';
import { Data, DataT } from '../myaddon/data.js';


export class DoubleValueRequest implements flatbuffers.IUnpackableObject<DoubleValueRequestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DoubleValueRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDoubleValueRequest(bb:flatbuffers.ByteBuffer, obj?:DoubleValueRequest):DoubleValueRequest {
  return (obj || new DoubleValueRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDoubleValueRequest(bb:flatbuffers.ByteBuffer, obj?:DoubleValueRequest):DoubleValueRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DoubleValueRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

version():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : 0;
}

config(obj?:Config):Config|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Config()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

data(index: number, obj?:Data):Data|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Data()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

dataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDoubleValueRequest(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addVersion(builder:flatbuffers.Builder, version:number) {
  builder.addFieldInt16(0, version, 0);
}

static addConfig(builder:flatbuffers.Builder, configOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, configOffset, 0);
}

static addData(builder:flatbuffers.Builder, dataOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, dataOffset, 0);
}

static createDataVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDoubleValueRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishDoubleValueRequestBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedDoubleValueRequestBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}


unpack(): DoubleValueRequestT {
  return new DoubleValueRequestT(
    this.version(),
    (this.config() !== null ? this.config()!.unpack() : null),
    this.bb!.createObjList<Data, DataT>(this.data.bind(this), this.dataLength())
  );
}


unpackTo(_o: DoubleValueRequestT): void {
  _o.version = this.version();
  _o.config = (this.config() !== null ? this.config()!.unpack() : null);
  _o.data = this.bb!.createObjList<Data, DataT>(this.data.bind(this), this.dataLength());
}
}

export class DoubleValueRequestT implements flatbuffers.IGeneratedObject {
constructor(
  public version: number = 0,
  public config: ConfigT|null = null,
  public data: (DataT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const config = (this.config !== null ? this.config!.pack(builder) : 0);
  const data = DoubleValueRequest.createDataVector(builder, builder.createObjectOffsetList(this.data));

  DoubleValueRequest.startDoubleValueRequest(builder);
  DoubleValueRequest.addVersion(builder, this.version);
  DoubleValueRequest.addConfig(builder, config);
  DoubleValueRequest.addData(builder, data);

  return DoubleValueRequest.endDoubleValueRequest(builder);
}
}
