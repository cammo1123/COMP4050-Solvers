// Generated by node/scripts/generate.mjs from fbs/operations/info.fbs. DO NOT EDIT.

import * as flatbuffers from "flatbuffers";

import {
	InfoRequestT as InfoRequestObject,
	InfoResponse as InfoResponseMessage,
	InfoResponseT as InfoResponseObject,
} from "./fbs.js";

export type InfoRequest = {

};

export type InfoResponse = {
	projectName: string;
	projectVersion: string;
	buildType: string;
	gitHash: string;
	gitBranch: string;
	buildTime: string;
	platform: string;
	arch: string;
	compiler: string;
	nodeVersion: string;
};

export function encodeRequest(request: InfoRequest): Uint8Array {

	const message = new InfoRequestObject(
		
	);

	const builder = new flatbuffers.Builder();
	builder.finish(message.pack(builder));
	return builder.asUint8Array();
}

export function decodeResponse(bytes: Uint8Array): InfoResponse {
	const message = InfoResponseMessage.getRootAsInfoResponse(new flatbuffers.ByteBuffer(bytes));
	const unpacked = message.unpack();
	return {
		projectName: unpacked.projectName as string,
		projectVersion: unpacked.projectVersion as string,
		buildType: unpacked.buildType as string,
		gitHash: unpacked.gitHash as string,
		gitBranch: unpacked.gitBranch as string,
		buildTime: unpacked.buildTime as string,
		platform: unpacked.platform as string,
		arch: unpacked.arch as string,
		compiler: unpacked.compiler as string,
		nodeVersion: unpacked.nodeVersion as string
	};
}
